### Session / User specific settings
#

### Auto-Start
#

exec mkdir -p "$XDG_RUNTIME_DIR/notifications"
exec mako

exec $HOME/Scripts/qutepy_launcher.py -k && $HOME/Scripts/list_desktop_applications.sh | $HOME/Scripts/qutepy_launcher.py -d -f -l -n

exec thunar --daemon
